{
  "name": "web-interface",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.1.1",
    "csshake": "^1.5.3",
    "prop-types": "^15.6.1",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-ionicons": "^2.1.6",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.4",
    "reactstrap": "^6.0.1",
    "redux": "^4.0.0"
  },
  "homepage": "http://samirbraga.github.io/CSV-Analytics",
  "scripts": {
    "start": "react-scripts start",
    "build": "rm public/CSV-Analytics && react-scripts build && ln -s /public public/CSV-Analytics",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "rm public/CSV-Analytics",
    "deploy": "gh-pages -b gh-pages -d build && ln -s /public public/CSV-Analytics"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
